version: 0.0
os: linux
files:
  - source: /
    destination: /home/erpuser/frappe-bench/apps/tag_workflow
hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 3600
      runas: root
